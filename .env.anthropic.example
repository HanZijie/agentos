# Copy to .env.anthropic.local (gitignored), then fill in your own values.
# Blank API key skips import and preserves the device's existing settings.
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=
ANTHROPIC_MODEL=
ANTHROPIC_MAX_TOKENS=4096
